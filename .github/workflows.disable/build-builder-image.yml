name: Build Builder Image

on:
  push:
    branches: [master]
    paths:
      - 'docker/Dockerfile.builder'
      - '.github/workflows/build-builder-image.yml'
      - 'CubeS3lvol/setup_dep.sh'
      - 'CubeS3lvol/patches/**'
  pull_request:
    paths:
      - 'docker/Dockerfile.builder'
      - '.github/workflows/build-builder-image.yml'
      - 'CubeS3lvol/setup_dep.sh'
      - 'CubeS3lvol/patches/**'
  workflow_dispatch:

concurrency:
  group: build-builder-image-${{ github.ref }}
  cancel-in-progress: false

env:
  IMAGE_NAME: ghcr.io/tencentcloud/cubesandbox-builder

jobs:
  build:
    name: Build ${{ matrix.arch }} builder image
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.repository == 'TencentCloud/CubeSandbox' && github.ref == 'refs/heads/master'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}

      - name: Prepare image tags
        id: image_tags
        env:
          ARCH: ${{ matrix.arch }}
          IS_RELEASE_PUSH: ${{ github.repository == 'TencentCloud/CubeSandbox' && github.ref == 'refs/heads/master' }}
        run: |
          short_sha="${GITHUB_SHA::7}"
          {
            echo "short_sha=${short_sha}"
            echo "tags<<EOF"
            echo "${IMAGE_NAME}:sha-${short_sha}-${ARCH}"
            if [[ "${IS_RELEASE_PUSH}" == "true" ]]; then
              echo "${IMAGE_NAME}:latest-${ARCH}"
              echo "${IMAGE_NAME}:ubuntu2004-${ARCH}"
            fi
            echo "EOF"
          } >> "${GITHUB_OUTPUT}"

      - name: Compute s3lvol dependency stamps
        id: s3lvol_stamps
        run: |
          {
            echo "spdk=$(CubeS3lvol/setup_dep.sh --print-stamp spdk)"
            echo "aws=$(CubeS3lvol/setup_dep.sh --print-stamp aws)"
          } >> "${GITHUB_OUTPUT}"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.builder
          platforms: ${{ matrix.platform }}
          push: ${{ github.repository == 'TencentCloud/CubeSandbox' && github.ref == 'refs/heads/master' }}
          tags: ${{ steps.image_tags.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            GITHUB_ACTIONS=true
            RUSTUP_DIST_SERVER=https://static.rust-lang.org
            RUSTUP_UPDATE_ROOT=https://static.rust-lang.org/rustup
            S3LVOL_SPDK_STAMP=${{ steps.s3lvol_stamps.outputs.spdk }}
            S3LVOL_AWS_STAMP=${{ steps.s3lvol_stamps.outputs.aws }}
          cache-from: type=gha,scope=builder-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=builder-${{ matrix.arch }}

  publish_manifest:
    name: Publish multi-arch builder manifests
    if: github.repository == 'TencentCloud/CubeSandbox' && github.ref == 'refs/heads/master'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-arch manifests
        run: |
          short_sha="${GITHUB_SHA::7}"
          docker buildx imagetools create \
            -t "${IMAGE_NAME}:latest" \
            "${IMAGE_NAME}:sha-${short_sha}-amd64" \
            "${IMAGE_NAME}:sha-${short_sha}-arm64"
          docker buildx imagetools create \
            -t "${IMAGE_NAME}:ubuntu2004" \
            "${IMAGE_NAME}:sha-${short_sha}-amd64" \
            "${IMAGE_NAME}:sha-${short_sha}-arm64"
          docker buildx imagetools create \
            -t "${IMAGE_NAME}:sha-${short_sha}" \
            "${IMAGE_NAME}:sha-${short_sha}-amd64" \
            "${IMAGE_NAME}:sha-${short_sha}-arm64"
