--- a/core/runtime/v2/binary.go	2026-08-30 19:48:02.220730347 +0800
+++ b/core/runtime/v2/binary.go	2026-09-08 00:38:57.265311898 +0800
@@ -27,6 +27,7 @@
 
 	"github.com/containerd/containerd/api/runtime/task/v2"
 	"github.com/containerd/containerd/v2/core/runtime"
+	"github.com/containerd/containerd/v2/pkg/cubeperf"
 	"github.com/containerd/containerd/v2/pkg/namespaces"
 	"github.com/containerd/containerd/v2/pkg/protobuf"
 	"github.com/containerd/containerd/v2/pkg/protobuf/proto"
@@ -64,8 +65,10 @@
 }
 
 func (b *binary) Start(ctx context.Context, opts *types.Any, onClose func()) (_ *shim, err error) {
+	cubeperf.Point(ctx, "binary-start-begin")
 	// containerd daemon is the intended caller of client.Command; the deprecation
 	// targets external callers.
+	stageStart := cubeperf.Now()
 	cmd, err := client.Command( //nolint:staticcheck // SA1019
 		ctx,
 		&client.CommandConfig{
@@ -83,6 +86,7 @@
 	if err != nil {
 		return nil, err
 	}
+	cubeperf.End(ctx, "shim-command-build", stageStart)
 	// Windows needs a namespace when openShimLog
 	ns, _ := namespaces.Namespace(ctx)
 	shimCtx, cancelShimLog := context.WithCancel(namespaces.WithNamespace(context.Background(), ns))
@@ -91,10 +95,12 @@
 			cancelShimLog()
 		}
 	}()
+	stageStart = cubeperf.Now()
 	f, err := openShimLog(shimCtx, b.bundle, client.AnonDialer)
 	if err != nil {
 		return nil, fmt.Errorf("open shim log pipe: %w", err)
 	}
+	cubeperf.End(ctx, "shim-log-open", stageStart)
 	defer func() {
 		if err != nil {
 			f.Close()
@@ -114,34 +120,44 @@
 			log.G(ctx).WithError(err).Error("copy shim log")
 		}
 	}()
+	stageStart = cubeperf.Now()
 	out, err := cmd.CombinedOutput()
 	if err != nil {
 		return nil, fmt.Errorf("%s: %w", out, err)
 	}
+	cubeperf.End(ctx, "shim-bootstrap-process", stageStart)
 	onCloseWithShimLog := func() {
 		onClose()
 		cancelShimLog()
 		f.Close()
 	}
 	// Save runtime binary path for restore.
+	stageStart = cubeperf.Now()
 	if err := os.WriteFile(filepath.Join(b.bundle.Path, "shim-binary-path"), []byte(b.runtime), 0600); err != nil {
 		return nil, err
 	}
+	cubeperf.End(ctx, "shim-binary-path-write", stageStart)
 
+	stageStart = cubeperf.Now()
 	params, err := parseStartResponse(out)
 	if err != nil {
 		return nil, err
 	}
+	cubeperf.End(ctx, "shim-bootstrap-parse", stageStart)
 
+	stageStart = cubeperf.Now()
 	conn, err := makeConnection(ctx, b.bundle.ID, params, onCloseWithShimLog, client.AnonDialer)
 	if err != nil {
 		return nil, err
 	}
+	cubeperf.End(ctx, "shim-connect", stageStart)
 
 	// Save bootstrap configuration (so containerd can restore shims after restart).
+	stageStart = cubeperf.Now()
 	if err := writeBootstrapParams(filepath.Join(b.bundle.Path, "bootstrap.json"), params); err != nil {
 		return nil, fmt.Errorf("failed to write bootstrap.json: %w", err)
 	}
+	cubeperf.End(ctx, "shim-bootstrap-write", stageStart)
 	// The address is in the form like ttrpc+unix://<uds-path> or grpc+vsock://<cid>:<port>
 	address := fmt.Sprintf("%s+%s", params.Protocol, params.Address)
 	return &shim{
--- a/core/runtime/v2/shim_manager.go	2026-08-30 19:48:02.221730344 +0800
+++ b/core/runtime/v2/shim_manager.go	2026-09-08 00:39:01.701300206 +0800
@@ -39,6 +39,7 @@
 	"github.com/containerd/containerd/v2/core/metadata"
 	"github.com/containerd/containerd/v2/core/runtime"
 	"github.com/containerd/containerd/v2/core/sandbox"
+	"github.com/containerd/containerd/v2/pkg/cubeperf"
 	"github.com/containerd/containerd/v2/pkg/namespaces"
 	shimbinary "github.com/containerd/containerd/v2/pkg/shim"
 	"github.com/containerd/containerd/v2/pkg/timeout"
@@ -307,6 +308,7 @@
 }
 
 func (m *ShimManager) startShim(ctx context.Context, bundle *Bundle, id string, opts runtime.CreateOpts) (*shim, error) {
+	cubeperf.Point(ctx, "shim-manager-start-begin")
 	ns, err := namespaces.NamespaceRequired(ctx)
 	if err != nil {
 		return nil, err
@@ -318,10 +320,12 @@
 		topts = opts.RuntimeOptions
 	}
 
+	stageStart := cubeperf.Now()
 	runtimePath, err := m.resolveRuntimePath(opts.Runtime)
 	if err != nil {
 		return nil, fmt.Errorf("failed to resolve runtime path: %w", err)
 	}
+	cubeperf.End(ctx, "runtime-path-resolve", stageStart)
 
 	b := shimBinary(bundle, shimBinaryConfig{
 		runtime:      runtimePath,
@@ -330,6 +334,7 @@
 		socketDir:    m.socketDir,
 		env:          m.env,
 	})
+	stageStart = cubeperf.Now()
 	shim, err := b.Start(ctx, typeurl.MarshalProto(topts), func() {
 		log.G(ctx).WithField("id", id).Info("shim disconnected")
 
@@ -343,6 +348,7 @@
 	if err != nil {
 		return nil, fmt.Errorf("start failed: %w", err)
 	}
+	cubeperf.End(ctx, "shim-binary-start", stageStart)
 
 	return shim, nil
 }
--- a/internal/cri/instrument/instrumented_service.go	2026-08-30 19:48:02.237730300 +0800
+++ b/internal/cri/instrument/instrumented_service.go	2026-09-08 00:39:01.701300206 +0800
@@ -24,6 +24,7 @@
 	"github.com/containerd/log"
 	runtime "k8s.io/cri-api/pkg/apis/runtime/v1"
 
+	"github.com/containerd/containerd/v2/pkg/cubeperf"
 	"github.com/containerd/containerd/v2/pkg/tracing"
 
 	ctrdutil "github.com/containerd/containerd/v2/internal/cri/util"
@@ -69,9 +70,14 @@
 	if err := in.checkInitialized(); err != nil {
 		return nil, err
 	}
+	if r.GetRuntimeHandler() == "cube" {
+		ctx = cubeperf.WithPod(ctx, r.GetConfig().GetMetadata().GetUid())
+		cubeperf.Point(ctx, "cri-receive")
+	}
 	span := tracing.SpanFromContext(ctx)
 	log.G(ctx).Infof("RunPodSandbox for %+v", r.GetConfig().GetMetadata())
 	defer func() {
+		cubeperf.Point(ctx, "cri-return")
 		if err != nil {
 			log.G(ctx).WithError(err).Errorf("RunPodSandbox for %+v failed, error", r.GetConfig().GetMetadata())
 		} else {
--- a/internal/cri/server/sandbox_run.go	2026-08-30 19:48:02.243730284 +0800
+++ b/internal/cri/server/sandbox_run.go	2026-09-08 00:39:01.701300206 +0800
@@ -40,6 +40,7 @@
 	"github.com/containerd/containerd/v2/internal/cri/server/podsandbox"
 	sandboxstore "github.com/containerd/containerd/v2/internal/cri/store/sandbox"
 	"github.com/containerd/containerd/v2/internal/cri/util"
+	"github.com/containerd/containerd/v2/pkg/cubeperf"
 	"github.com/containerd/containerd/v2/pkg/netns"
 	"github.com/containerd/containerd/v2/pkg/tracing"
 )
@@ -67,6 +68,8 @@
 		return nil, errors.New("sandbox config must include metadata")
 	}
 	name := makeSandboxName(metadata)
+	ctx = cubeperf.WithSandbox(ctx, id)
+	cubeperf.Point(ctx, "id-generated")
 
 	span.SetAttributes(
 		tracing.Attribute("sandbox.id", id),
@@ -82,9 +85,11 @@
 
 	// Reserve the sandbox name to avoid concurrent `RunPodSandbox` request starting the
 	// same sandbox.
+	stageStart := cubeperf.Now()
 	if err := c.sandboxNameIndex.Reserve(name, id); err != nil {
 		return nil, fmt.Errorf("failed to reserve sandbox name %q: %w", name, err)
 	}
+	cubeperf.End(ctx, "name-reserve", stageStart)
 	defer func() {
 		// Release the name if the function returns with an error.
 		// When cleanupErr != nil, the name will be cleaned in sandbox_remove.
@@ -94,10 +99,12 @@
 	}()
 
 	leaseSvc := c.client.LeasesService()
+	stageStart = cubeperf.Now()
 	ls, lerr := leaseSvc.Create(ctx, leases.WithID(id))
 	if lerr != nil {
 		return nil, fmt.Errorf("failed to create lease for sandbox name %q: %w", name, lerr)
 	}
+	cubeperf.End(ctx, "lease-create", stageStart)
 	defer func() {
 		if retErr != nil {
 			deferCtx, deferCancel := util.DeferContext()
@@ -114,16 +121,19 @@
 		sandboxInfo = sb.Sandbox{ID: id}
 	)
 
+	stageStart = cubeperf.Now()
 	ociRuntime, err := c.config.GetSandboxRuntime(config, r.GetRuntimeHandler())
 	if err != nil {
 		return nil, fmt.Errorf("unable to get OCI runtime for sandbox %q: %w", id, err)
 	}
+	cubeperf.End(ctx, "runtime-resolve", stageStart)
 
 	sandboxInfo.Runtime.Name = ociRuntime.Type
 	sandboxInfo.Sandboxer = ociRuntime.Sandboxer
 
 	runtimeStart := time.Now()
 	// Retrieve runtime options
+	stageStart = cubeperf.Now()
 	runtimeOpts, err := criconfig.GenerateRuntimeOptions(ociRuntime)
 	if err != nil {
 		return nil, fmt.Errorf("failed to generate sandbox runtime options: %w", err)
@@ -135,6 +145,7 @@
 			return nil, fmt.Errorf("failed to marshal runtime options: %w", err)
 		}
 	}
+	cubeperf.End(ctx, "runtime-options", stageStart)
 
 	// Save sandbox name
 	sandboxInfo.AddLabel("name", name)
@@ -158,9 +169,11 @@
 		return nil, fmt.Errorf("unable to update extensions for sandbox %q: %w", id, err)
 	}
 
+	stageStart = cubeperf.Now()
 	if _, err := c.client.SandboxStore().Create(ctx, sandboxInfo); err != nil {
 		return nil, fmt.Errorf("failed to save sandbox metadata: %w", err)
 	}
+	cubeperf.End(ctx, "sandbox-store-create", stageStart)
 	defer func() {
 		if retErr != nil && cleanupErr == nil {
 			cleanupErr = c.client.SandboxStore().Delete(ctx, id)
@@ -206,14 +219,17 @@
 		}
 
 		if !userNsEnabled {
+			stageStart = cubeperf.Now()
 			sandbox.NetNS, err = netns.NewNetNS(netnsMountDir)
 		} else {
+			stageStart = cubeperf.Now()
 			usernsOpts := config.GetLinux().GetSecurityContext().GetNamespaceOptions().GetUsernsOptions()
 			sandbox.NetNS, err = c.setupNetnsWithinUserns(netnsMountDir, usernsOpts)
 		}
 		if err != nil {
 			return nil, fmt.Errorf("failed to create network namespace for sandbox %q: %w", id, err)
 		}
+		cubeperf.End(ctx, "netns-create", stageStart)
 		// Update network namespace in the store, which is used to generate the container's spec
 		sandbox.NetNSPath = sandbox.NetNS.GetPath()
 		defer func() {
@@ -231,9 +247,11 @@
 			return nil, fmt.Errorf("unable to update extensions for sandbox %q: %w", id, err)
 		}
 		// Save sandbox metadata to store
+		stageStart = cubeperf.Now()
 		if sandboxInfo, err = c.client.SandboxStore().Update(ctx, sandboxInfo, "extensions"); err != nil {
 			return nil, fmt.Errorf("unable to save sandbox %q to sandbox store: %w", id, err)
 		}
+		cubeperf.End(ctx, "sandbox-store-netns-update", stageStart)
 		// Define this defer to teardownPodNetwork prior to the setupPodNetwork function call.
 		// This is because in setupPodNetwork the resource is allocated even if it returns error, unlike other resource
 		// creation functions.
@@ -263,9 +281,11 @@
 		// In this case however caching the IP will add a subtle performance enhancement by avoiding
 		// calls to network namespace of the pod to query the IP of the veth interface on every
 		// SandboxStatus request.
+		stageStart = cubeperf.Now()
 		if err := c.setupPodNetwork(ctx, &sandbox); err != nil {
 			return nil, fmt.Errorf("failed to setup network for sandbox %q: %w", id, err)
 		}
+		cubeperf.End(ctx, "cni-setup", stageStart)
 		sandboxCreateNetworkTimer.UpdateSince(netStart)
 
 		if err := sandboxInfo.AddExtension(podsandbox.MetadataKey, &sandbox.Metadata); err != nil {
@@ -273,14 +293,18 @@
 		}
 
 		// Save sandbox metadata to store
+		stageStart = cubeperf.Now()
 		if sandboxInfo, err = c.client.SandboxStore().Update(ctx, sandboxInfo, "extensions"); err != nil {
 			return nil, fmt.Errorf("unable to save sandbox %q to sandbox store: %w", id, err)
 		}
+		cubeperf.End(ctx, "sandbox-store-network-update", stageStart)
 	}
 
+	stageStart = cubeperf.Now()
 	if err := c.sandboxService.CreateSandbox(ctx, sandboxInfo, sb.WithOptions(config), sb.WithNetNSPath(sandbox.NetNSPath)); err != nil {
 		return nil, fmt.Errorf("failed to create sandbox %q: %w", id, err)
 	}
+	cubeperf.End(ctx, "controller-create", stageStart)
 
 	// HACK: Ensure pause container image is present before starting the sandbox.
 	// Ideally, this should be called from the sandbox implementation itself, but it's
@@ -292,10 +316,13 @@
 	// containers anyway, the CRI layer will pre-pull the pause container to guarantee
 	// it exists (even though it's counter to the purpose of the sandbox API). This may
 	// be removed/deprecated in the distant future, if we decide to remove pause containers.
+	stageStart = cubeperf.Now()
 	if err := c.ensurePauseImageExists(ctx, r.GetConfig(), r.GetRuntimeHandler()); err != nil {
 		return nil, err
 	}
+	cubeperf.End(ctx, "pause-image", stageStart)
 
+	stageStart = cubeperf.Now()
 	ctrl, err := c.sandboxService.StartSandbox(ctx, sandbox.Sandboxer, id)
 	if err != nil {
 		var cerr podsandbox.CleanupErr
@@ -311,6 +338,7 @@
 		}
 		return nil, fmt.Errorf("failed to start sandbox %q: %w", id, err)
 	}
+	cubeperf.End(ctx, "controller-start", stageStart)
 
 	// Shutdown the sandbox if we fail before adding it to store.
 	rollbackSandbox := true
@@ -332,9 +360,11 @@
 	sandboxInfo.Labels = ctrl.Labels
 	sandboxInfo.Spec = ctrl.Spec
 
+	stageStart = cubeperf.Now()
 	if sandboxInfo, err = c.client.SandboxStore().Update(ctx, sandboxInfo, "extensions", "spec", "labels"); err != nil {
 		return nil, fmt.Errorf("unable to save sandbox %q to sandbox store: %w", id, err)
 	}
+	cubeperf.End(ctx, "sandbox-store-start-update", stageStart)
 
 	labels := ctrl.Labels
 	if labels == nil {
@@ -353,10 +383,12 @@
 
 	defer c.nri.BlockPluginSync().Unblock()
 
+	stageStart = cubeperf.Now()
 	err = c.nri.RunPodSandbox(ctx, &sandbox)
 	if err != nil {
 		return nil, fmt.Errorf("NRI RunPodSandbox failed: %w", err)
 	}
+	cubeperf.End(ctx, "nri-run", stageStart)
 
 	defer func() {
 		if retErr != nil {
@@ -375,9 +407,11 @@
 	}
 
 	// Add sandbox into sandbox store in INIT state.
+	stageStart = cubeperf.Now()
 	if err := c.sandboxStore.Add(sandbox); err != nil {
 		return nil, fmt.Errorf("failed to add sandbox %+v into store: %w", sandbox, err)
 	}
+	cubeperf.End(ctx, "sandbox-cache-add", stageStart)
 	// We no longer need to stop sandbox with a cleanup defer since it is in the store.
 	rollbackSandbox = false
 
@@ -386,10 +420,12 @@
 	// SandboxStatus from the store and include it in the event.
 	c.generateAndSendContainerEvent(ctx, id, id, runtime.ContainerEventType_CONTAINER_CREATED_EVENT)
 
+	stageStart = cubeperf.Now()
 	exitCh, err := c.sandboxService.WaitSandbox(util.NamespacedContext(), sandbox.Sandboxer, id)
 	if err != nil {
 		return nil, fmt.Errorf("failed to wait sandbox %s: %v", id, err)
 	}
+	cubeperf.End(ctx, "sandbox-wait-register", stageStart)
 
 	// start the monitor after adding sandbox into the store, this ensures
 	// that sandbox is in the store, when event monitor receives the TaskExit event.
@@ -402,6 +438,7 @@
 	c.generateAndSendContainerEvent(ctx, id, id, runtime.ContainerEventType_CONTAINER_STARTED_EVENT)
 
 	sandboxRuntimeCreateTimer.WithValues(labels["oci_runtime_type"]).UpdateSince(runtimeStart)
+	cubeperf.Point(ctx, "server-complete")
 
 	return &runtime.RunPodSandboxResponse{PodSandboxId: id}, nil
 }
--- a/pkg/cubeperf/clock_linux.go	1970-01-01 08:00:00.000000000 +0800
+++ b/pkg/cubeperf/clock_linux.go	2026-09-08 00:39:01.699300211 +0800
@@ -0,0 +1,27 @@
+//go:build linux
+
+// Copyright The containerd Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package cubeperf
+
+import "golang.org/x/sys/unix"
+
+func monotonicMicros() int64 {
+	var ts unix.Timespec
+	if err := unix.ClockGettime(unix.CLOCK_MONOTONIC, &ts); err != nil {
+		return 0
+	}
+	return ts.Sec*1_000_000 + ts.Nsec/1_000
+}
--- a/pkg/cubeperf/clock_other.go	1970-01-01 08:00:00.000000000 +0800
+++ b/pkg/cubeperf/clock_other.go	2026-09-08 00:38:01.532458796 +0800
@@ -0,0 +1,21 @@
+//go:build !linux
+
+// Copyright The containerd Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package cubeperf
+
+func monotonicMicros() int64 {
+	return 0
+}
--- a/pkg/cubeperf/trace.go	1970-01-01 08:00:00.000000000 +0800
+++ b/pkg/cubeperf/trace.go	2026-09-08 00:39:01.699300211 +0800
@@ -0,0 +1,104 @@
+// Copyright The containerd Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+// Package cubeperf provides an opt-in monotonic timeline for the CubeSandbox
+// PoC. It is inert unless CUBE_PERF_TRACE=1 and the request is explicitly
+// associated with the cube runtime handler.
+package cubeperf
+
+import (
+	"context"
+	"os"
+
+	"github.com/containerd/log"
+)
+
+type operation struct {
+	podUID    string
+	sandboxID string
+}
+
+type operationKey struct{}
+
+var enabled = os.Getenv("CUBE_PERF_TRACE") == "1"
+
+// WithPod starts a trace context for one Cube RunPodSandbox request.
+func WithPod(ctx context.Context, podUID string) context.Context {
+	if !enabled || podUID == "" {
+		return ctx
+	}
+	return context.WithValue(ctx, operationKey{}, operation{podUID: podUID})
+}
+
+// WithSandbox binds the containerd-generated sandbox ID to the request trace.
+func WithSandbox(ctx context.Context, sandboxID string) context.Context {
+	if !enabled || sandboxID == "" {
+		return ctx
+	}
+	op, ok := ctx.Value(operationKey{}).(operation)
+	if !ok {
+		return ctx
+	}
+	op.sandboxID = sandboxID
+	return context.WithValue(ctx, operationKey{}, op)
+}
+
+// Enabled reports whether a request has opted into the Cube performance trace.
+func Enabled(ctx context.Context) bool {
+	if !enabled {
+		return false
+	}
+	_, ok := ctx.Value(operationKey{}).(operation)
+	return ok
+}
+
+// Now returns CLOCK_MONOTONIC microseconds, or zero on unsupported platforms.
+func Now() int64 {
+	if !enabled {
+		return 0
+	}
+	return monotonicMicros()
+}
+
+// Point emits a request-correlated point in the host CLOCK_MONOTONIC domain.
+func Point(ctx context.Context, phase string) {
+	if !Enabled(ctx) {
+		return
+	}
+	now := monotonicMicros()
+	if now == 0 {
+		return
+	}
+	op := ctx.Value(operationKey{}).(operation)
+	log.G(ctx).Infof(
+		"cube_perf component=containerd operation=run-pod-sandbox phase=%s sandbox_id=%s pod_uid=%s ts_mono_us=%d",
+		phase, op.sandboxID, op.podUID, now,
+	)
+}
+
+// End emits the endpoint and duration of a stage begun with Now.
+func End(ctx context.Context, phase string, started int64) {
+	if !Enabled(ctx) || started == 0 {
+		return
+	}
+	now := monotonicMicros()
+	if now == 0 {
+		return
+	}
+	op := ctx.Value(operationKey{}).(operation)
+	log.G(ctx).Infof(
+		"cube_perf component=containerd operation=run-pod-sandbox phase=%s sandbox_id=%s pod_uid=%s ts_mono_us=%d duration_us=%d",
+		phase, op.sandboxID, op.podUID, now, now-started,
+	)
+}
--- a/plugins/sandbox/controller.go	2026-08-30 19:48:02.255730251 +0800
+++ b/plugins/sandbox/controller.go	2026-09-08 00:38:56.949312731 +0800
@@ -39,6 +39,7 @@
 	"github.com/containerd/containerd/v2/core/runtime"
 	v2 "github.com/containerd/containerd/v2/core/runtime/v2"
 	"github.com/containerd/containerd/v2/core/sandbox"
+	"github.com/containerd/containerd/v2/pkg/cubeperf"
 	"github.com/containerd/containerd/v2/plugins"
 )
 
@@ -123,24 +124,30 @@
 func (c *controllerLocal) Create(ctx context.Context, info sandbox.Sandbox, opts ...sandbox.CreateOpt) (retErr error) {
 	var coptions sandbox.CreateOptions
 	sandboxID := info.ID
+	cubeperf.Point(ctx, "controller-create-begin")
 	for _, opt := range opts {
 		opt(&coptions)
 	}
 
+	stageStart := cubeperf.Now()
 	if _, err := c.shims.Get(ctx, sandboxID); err == nil {
 		return fmt.Errorf("sandbox %s already running: %w", sandboxID, errdefs.ErrAlreadyExists)
 	}
+	cubeperf.End(ctx, "shim-cache-probe", stageStart)
 
+	stageStart = cubeperf.Now()
 	bundle, err := v2.NewBundle(ctx, c.root, c.state, sandboxID, info.Spec)
 	if err != nil {
 		return err
 	}
+	cubeperf.End(ctx, "bundle-create", stageStart)
 	defer func() {
 		if retErr != nil {
 			bundle.Delete()
 		}
 	}()
 
+	stageStart = cubeperf.Now()
 	shim, err := c.shims.Start(ctx, sandboxID, bundle, runtime.CreateOpts{
 		Spec:           info.Spec,
 		RuntimeOptions: info.Runtime.Options,
@@ -150,12 +157,16 @@
 	if err != nil {
 		return fmt.Errorf("failed to start new shim for sandbox %s: %w", sandboxID, err)
 	}
+	cubeperf.End(ctx, "shim-manager-start", stageStart)
 
+	stageStart = cubeperf.Now()
 	svc, err := sandbox.NewClient(shim.Client())
 	if err != nil {
 		return err
 	}
+	cubeperf.End(ctx, "sandbox-client", stageStart)
 
+	stageStart = cubeperf.Now()
 	if _, err := svc.CreateSandbox(ctx, &runtimeAPI.CreateSandboxRequest{
 		SandboxID:   sandboxID,
 		BundlePath:  shim.Bundle(),
@@ -167,26 +178,35 @@
 		c.cleanupShim(ctx, sandboxID, svc)
 		return fmt.Errorf("failed to create sandbox %s: %w", sandboxID, errgrpc.ToNative(err))
 	}
+	cubeperf.End(ctx, "shim-create-rpc", stageStart)
+	cubeperf.Point(ctx, "controller-create-complete")
 
 	return nil
 }
 
 func (c *controllerLocal) Start(ctx context.Context, sandboxID string) (sandbox.ControllerInstance, error) {
+	cubeperf.Point(ctx, "controller-start-begin")
+	stageStart := cubeperf.Now()
 	shim, err := c.shims.Get(ctx, sandboxID)
 	if err != nil {
 		return sandbox.ControllerInstance{}, fmt.Errorf("unable to find sandbox %q", sandboxID)
 	}
+	cubeperf.End(ctx, "start-shim-cache-get", stageStart)
 
+	stageStart = cubeperf.Now()
 	svc, err := sandbox.NewClient(shim.Client())
 	if err != nil {
 		return sandbox.ControllerInstance{}, err
 	}
+	cubeperf.End(ctx, "start-sandbox-client", stageStart)
 
+	stageStart = cubeperf.Now()
 	resp, err := svc.StartSandbox(ctx, &runtimeAPI.StartSandboxRequest{SandboxID: sandboxID})
 	if err != nil {
 		c.cleanupShim(ctx, sandboxID, svc)
 		return sandbox.ControllerInstance{}, fmt.Errorf("failed to start sandbox %s: %w", sandboxID, errgrpc.ToNative(err))
 	}
+	cubeperf.End(ctx, "shim-start-rpc", stageStart)
 	address, version := shim.Endpoint()
 	return sandbox.ControllerInstance{
 		SandboxID: sandboxID,

