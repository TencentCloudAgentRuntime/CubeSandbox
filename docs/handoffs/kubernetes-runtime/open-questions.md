# Kubernetes RuntimeClass PoC 未决问题

| ID | 问题 | 当前假设 | Owner | 最迟 Stage | 状态 | 所需证据/最终决定 |
|---|---|---|---|---|---|---|
| K8S-OQ-001 | containerd 2.3 Sandbox API 的实际 CRI 调用顺序和配置字段是什么？ | 使用 `sandboxer = "shim"`，CubeShim 同时承载 Sandbox/Task | Codex | S0 | DECIDED | handler 使用 `runtime_type/runtime_path/sandboxer = "shim"`；bootstrap v3 返回 ttrpc endpoint，Sandbox/Task 同 PID；CNI ADD 在 Create 前，Stop 后 CNI DEL，再 Shutdown/delete。正常与四类异常的 10 类残留均为 0，证据 `inv-68246d0jt1` 和 `evidence/s0.1/` |
| K8S-OQ-002 | VM 启动后新增 bind mount 是否能被固定 virtiofs shared root 稳定看到？ | 每 Pod 一个固定 shared root 可支持动态容器/卷 | Codex | S0 | DECIDED | `cache=never/read_only=true/announce_submounts=false` 下 bind、rename、只读立即可见；普通 live unmount 可能因 virtiofsd inode 引用 `EBUSY`，必须 Guest-first 解除消费、Host `MNT_DETACH`、generation 路径不复用。实测 Guest 对非根目录仍有 86400 秒 entry/attribute timeout，因此 Pod VM 存活期间还必须保留 managed `rootfs` 父目录 inode，只替换/删除内部 generation；连续两个真实 Task 已通过。证据 `inv-9827ikgt4f`、`inv-9837xq0wnq` |
| K8S-OQ-003 | PoC 网络首先采用哪种数据面？ | 先用 Cilium，保留 VPC-CNI/Global Router adapter | Codex | S0 | DECIDED | Cilium 1.20.0 tcfilter 已实测 Pod IP/MAC/MTU、Cluster DNS、Service ClusterIP、跨节点 PodIP 和 egress NetworkPolicy；成功/失败资源残留均为 0。VPC-CNI/Global Router 作为后续 adapter 兼容项；证据 `inv-a82g9g0x1f` 和 `evidence/s0.3/` |
| K8S-OQ-004 | Kubernetes 路径能否完全使用标准 `CreateTaskRequest.rootfs`？ | 可以，legacy 私有注解只服务原链路 | Codex | S0 | DECIDED | 可以。CubeShim 解析标准 overlay active snapshot，按 active upper + image lowers 原序导出并内部适配当前 Agent；CRI/containerd 不提供私有 rootfs 输入。S1.2 已在 containerd 2.3.4 Task API v3 下连续运行自然退出 23 和 SIGKILL 137 的真实 Task；删除后 container/Task/Sandbox/Task 新增 snapshot/mount 残留均为 0，验收前后的既有 snapshot 集合一致。Guest 临时 upper 不回写 containerd 的限制在 S3 处理；证据 `inv-9827ikgt4f`、`inv-9837xq0wnq` |
| K8S-OQ-005 | Cubelet 节点 RPC 的最小边界是什么？ | 只做 VM/网络资产准备、释放和对账 | Codex | S0 | DECIDED | `runtime.v1.RuntimeResource` 仅含 capability、Prepare、Release、Inspect 和 report-only Reconcile；host containerd 独占 CRI/image/snapshot/Sandbox/Task，Cubelet service 禁止回入内嵌 containerd；TAP FD 经 cubetap `SCM_RIGHTS` 交付。证据 `40f4389a` 和 `evidence/s0.4/` |
| K8S-OQ-006 | PoC VM 规格如何确定？ | 固定 1 vCPU/256 MiB；生产再做资源聚合 | Codex | S1 | DECIDED | CRI 无显式资源时的默认推导单测已纳入 `inv-a83cu30ran` 严格构建；S1.4 的 Job、Deployment、取消和 100 Pod 矩阵均以该规格启动真实 Cube VM。生产密度与规格调优在 S5 性能验收处理 |
| K8S-OQ-007 | ConfigMap/Secret 动态更新能否透过 virtiofs 保持语义？ | 使用独立 `cubeVolumes cache=none` share 传播 kubelet atomic-writer 更新；subPath 保持创建时 inode | Codex | S3.1c | DECIDED | S3.1c `20881f71` / `inv-a83th50gfh` 验证 ConfigMap、Secret、projected、downwardAPI 的启动值、mode 和动态更新：runc 1 秒、Cube 0 秒读到完整 `v2`，Host/Guest `..data` target 与 inode 均变化；writer/peer、Sandbox、shim、VM、Task generation 和 mount ID 稳定。两种 runtime 的 subPath 均保持 `v1` 与原 inode；删除后恢复全量基线，证据见 `evidence/s3.1/` |
| K8S-OQ-008 | Snapshot/Restore CRD 和 artifact 格式如何定版？ | 二期优先从快照创建新 Pod | 待指定 | S6 | DEFERRED | 多容器一致性、远端存储和 CSI snapshot PoC |
| K8S-OQ-009 | Sandbox API 的 `Spec` 应返回什么 protobuf `Any.type_url`？ | 返回 OCI Spec JSON 的 containerd 标准 Any | Codex | S1.4 | DECIDED | `type_url=types.containerd.io/opencontainers/runtime-spec/1/Spec`，`value` 为 OCI JSON；`ctr` 解码、CRI inspect 和 containerd warning=0，证据 `inv-983d0j0cbd`、`evidence/s1.4/` |
| K8S-OQ-010 | RuntimeResource 已释放 lease 的 durable tombstone 如何做保留和压缩？ | PoC 为 stale Release/Prepare fencing 每个 Sandbox 保留一条释放记录；它不是 active lease 或运行资源，但生产版本需要有界保留 | 待指定 | S5 | OPEN | 定义保留窗口/压缩水位，验证节点重启与迟到 RPC 不会复活旧 generation，并做长稳磁盘增长测试 |
| K8S-OQ-011 | Kubernetes 可写 bind volume 如何在固定只读 virtio-fs share 中保持 `rw` 语义？ | 使用 Pod 级独立 `cubeVolumes` share：rootfs `cube.fs` 保持只读，Host Volume 通道可写，Guest 按标准 OCI mount 保持逐项 `ro/rw` | Codex | S3.1 | DECIDED | S3.1b `5b504b58` 已实现固定 Volume root 与 per-Task generation；`inv-a83rxbgg0g` 通过 137 项 Rust、Go race/vet 和 privileged bind cleanup；`inv-a83sdvgumu` 验证 disk/memory emptyDir 跨 init/app/sidecar 读写、同 source 的 `rw/ro`、只读 rootfs、Task/失败回滚清理和 tombstone +2，`inv-883sh80mpp` 独立审计通过 |
| K8S-OQ-012 | ephemeral container 的 `targetContainerName` 是否需要加入目标容器 PID namespace？ | 首版支持标准动态加入、独立 PID namespace 与 `shareProcessNamespace`；不把 TARGET PID 能力伪装为已支持 | 待指定 | S5.3 | OPEN | S2.4 `inv-983kt80r3f` 已证明 API 请求保留 `targetContainerName=app`，但 debugger 看不到目标 PID；S5.3 结合 Node E2E/调试场景决定新增 per-target PID join，还是作为明确兼容性限制 |
| K8S-OQ-013 | OCI `noNewPrivileges=true` 如何无损进入 Cube Guest？ | 移除 CubeShim protobuf 转换中的强制 `false`，保持 Agent 兼容并由 Guest 在启动进程前设置 NNP；不能静默降级 | Codex | S3.3d | DECIDED | `5a851245` 移除 NNP 强制清零并保持当前 RuntimeDefault seccomp 子集；对 protobuf 无法无损表示的 `defaultErrnoRet`、`listenerPath`、`listenerMetadata` 和显式 syscall `errnoRet=0` 在 Host Shim fail-closed。`inv-b8497x0qge` 完成最终 Shim 构建与全量测试；`inv-b849phgapi` 两轮验证 runc/Cube 的 NNP `false/true`、Guest `NoNewPrivs=0/1`、seccomp mode `0/2` 和 `unshare` 允许/阻断，lease `488→492` 且精确恢复基线；`inv-3849tjgnmx` 独立审计通过 |
| K8S-OQ-014 | 标准 Pod/container resources 如何分配到 Host Pod VM cgroup 与 Guest per-container cgroup，且不重复或遗漏约束？ | kubelet 独占 Pod parent；Cube leaf 只表达静态 VM capacity+RuntimeClass overhead；Guest resource parent 保留 OCI per-container 限制 | Codex | S3.4c | DECIDED | S3.4b 已关闭 Guest create/update；S3.4c 的 managed Sandbox 把 Shim/VMM/virtiofs 置于 OCI 指定 leaf。最终 `2a23aa3c` 上的 `inv-a86i6rgrkc`、`inv-686i7a0qux`、`inv-686i7a0quv`、`inv-386i7ags0q` 覆盖 BestEffort/Burstable/Guaranteed、classic init/restartable sidecar/app 与 Pod-level 的六 Pod、九容器三层 readback；原始逐对象证据及 SHA-256 随 handoff 保存。Host CPU/memory/PIDs、Guest OOM、survivor 和 exact baseline 通过。Cube 不重算 aggregate、不写 parent，以祖先/leaf 交集执行 |
| K8S-OQ-015 | `UpdateContainerResources` 与 Kubernetes in-place Pod resize 在 Cube VM 中如何同时更新 Host/Guest 两层限制？ | 不做跨 owner 伪事务：kubelet 更新 Pod parent，Task Update 更新 Guest 目标容器，Cube static leaf 不随 workload resize 重算 | Codex | S3.4d | DECIDED | containerd 2.3.4 不转发 sandbox Update，但 kubelet 先更新 parent。S3.4c 证明预留容量内 parent 自动更新、leaf 不变、Guest 精确更新；`inv-v86ksj0ndb` 又覆盖 containerd restart 后两次 resize、identity/restart/HTTP 稳定。超过静态容量转 K8S-OQ-018；持续 long-exec 并发限制转 K8S-OQ-022 |
| K8S-OQ-016 | CPU/memory 之外的 swap、PIDs、hugepages、ephemeral-storage 分别应由哪一层执行？ | swap/hugepage 与 workload PIDs 属于 Guest per-container；Host leaf 仅用独立 `pids.max` 保护 Shim/VMM threads；ephemeral-storage 由 kubelet 对 Host emptyDir/containerd snapshot 计量并驱逐 | Codex | S3.4d | DECIDED | S3.4b 已关闭 Guest swap/PIDs/hugepage 协议与压力边界；有限 Pod PID accounting 转 K8S-OQ-019。`inv-386mfegiev` 中 Cube Pod 写入 96Mi emptyDir、容器总限额 16Mi 后被 kubelet 标准判定 `Failed/Evicted`，Node `DiskPressure=False`；namespace 删除，`inv-086mip0u7c` W2 exact-zero。Guest 临时 rootfs upper 不回写 containerd，故首版 ephemeral-storage 支持面只声明 Host-backed emptyDir/标准 snapshot 可计量路径 |
| K8S-OQ-017 | checkBeforeUpdate=false 时，极端 `memory.max` 下调如何避免内核同步 reclaim 超过 Shim-Agent RPC deadline，并在未知完成状态下对账？ | Kubernetes owner 由 kubelet 在 CRI 前按当前用量保护；managed 直接 CRI 缺省为写前检查；只有显式 false 保留低层语义 | Codex | S3.4d | DECIDED | `5b4b15dc` 定向测试证明缺省 managed downsize 使用 check=true；`inv-a86ket058u` 证明 Kubernetes 1.36 在 CRI 前拒绝/延迟低于当前 usage 的下调且无 deadline wedge。首轮 `inv-986kd00166` 仅因错误要求 Agent 事件失败。显式 false 的低层同步 reclaim 风险不属于首版 Kubernetes 支持面 |
| K8S-OQ-018 | in-place resize 后 Pod aggregate 超过创建时静态 VM vCPU/memory 容量时如何处理？ | 首版只验收创建时通过 `cube.vmmres` 预留足够容量的 resize；不把容器 limit 更新等同于 VM hotplug | 待指定 | S6 | DEFERRED | 二期比较 VM CPU/memory hotplug、recreate/restore 与 containerd sandbox Update/status 扩展；在实现前，超过静态容量不声明支持 |
| K8S-OQ-019 | kubelet 配置有限 `podPidsLimit` 时，如何区分 Host VM threads 与 Guest workload PID 预算？ | PoC 使用 `podPidsLimit=-1`；Cube leaf 的 512 是 Host runtime safety ceiling，Guest OCI PIDs 是 workload 限制，二者不相减 | 待指定 | S5 | OPEN | 需要定义跨 VM 的 Pod PID accounting/API，验证 scheduler/kubelet eviction 与 Guest PID 汇总；完成前不支持有限 kubelet Pod PID 语义 |
| K8S-OQ-020 | RuntimeResource Release 遇到 systemd scope 已 inactive/消失时是否应视为幂等成功？ | scope 已无进程且 owner identity 与期望一致时按 already-gone 收敛；身份不确定仍 fail closed | Codex | S3.4c | DECIDED | `2a23aa3c` 仅在 owner identity 匹配、unit 确认 inactive/dead，且 cgroup 精确为空或已消失时幂等成功；active、populated、identity mismatch 和查询错误仍 fail closed。`4066b17d/6662316e` 覆盖非零退出但 stdout=`not-found` 与 1 秒内收集重读；`2dcf26b9/10b7af56` 在等待后、空 leaf 检查前后及 reset 前重验 identity，替代 leaf 定向测试随 `inv-v86nregacb` 通过。普通 Kubernetes 删除 `inv-b86i5egc5x` 无 `FailedKillPod`；超过重读窗口的创建即取消边界单列 `K8S-OQ-023` |
| K8S-OQ-021 | 默认小 VM 规格下并发启动多个启动即高内存负载的 Pod，`ReleasePending` 的容量边界和可诊断语义是什么？ | VM memory 至少 256 MiB、managed Host leaf 至少 512 MiB；更小显式请求 fail-fast，Guest workload limit 独立执行 | Codex | S3.4d | DECIDED | `7526fc29` 实现 floor；`inv-686k7g02nv` 默认规格 8/8 首次 `OOMKilled/137`；`inv-b86k9w0j42`/`inv-986k9vg064` 活动期 leaf=512MiB 且 Host oom=0；`inv-986k8v0uwi` 显式 64MiB 请求明确拒绝；删除与 exact-zero 通过 |
| K8S-OQ-022 | 长时间 exec 与 `UpdateContainerResources` 并发时，Agent 操作通道是否会阻塞并导致状态分叉？ | Shim RPC handle 不跨 await 加锁；Agent 同容器长操作彻底解串行延期到 S5.4，首版不声明支持持续长 exec+resize 组合 | Codex | S5.4 | DEFERRED | `011a05fa` 修复 Shim client mutex；`d40d9e07` 增加 clone-safe container gate 与 sandbox fence，确定性覆盖 start/update 与 delete/kill/pause。`inv-v86ksj0ndb` 的受控组合通过；激进压力 `inv-086kus0i8w` 仍首次 Agent Update/探针超时，但约 53 秒后收敛。`inv-086ncr074p` 的 exec+连续 resize+delete 在 0 秒删除且无 FailedKillPod。剩余 Agent 锁粒度作为 P1 已知限制转 S5.4 |
| K8S-OQ-023 | 多个 Sandbox 创建刚开始就同时取消时，systemd scope collection 如何避免瞬时 `FailedKillPod`？ | 保持精确身份与空 leaf fail-closed；kubelet 重试可收敛，首版允许可诊断的瞬时告警 | Codex | S5.4 | DEFERRED | `inv-086ncr074p` 中 12 Pod 创建 2 秒即删除全部成功但产生 3 个 scope collection `FailedKillPod`；`4066b17d/6662316e` 已覆盖明确 absent 与 1 秒内收集，`2dcf26b9/10b7af56` 保证等待期间 identity 变化时 fail-closed，但压力证明更长 collection 窗口仍存在。随后 `inv-a86ndt0pkt`/`inv-a86nds0c3q` 双 Worker exact-zero、`inv-v86nea09kv` 3/3 Ready。S5.4 评估异步 release/reaper 或延长/事件化等待并补 churn 零告警门禁 |
