# S3.4c.3 RuntimeClass overhead 与静态 leaf ceiling 执行摘要

状态：`DONE`。同一 reviewer 已明确返回 `APPROVE S3.4c.3`。实现基线为 `be91f9e0dac3b0a1f963aade71d8fb45c67a7529`，由 `b609a42b`、`5868385e`、`17fbbfa1`、`6c7eb754` 和 `be91f9e0` 五个实现提交组成。

## 固定构建与自动化验证

- 最终固定 builder 调用 `inv-386fp40a6b`：`service::` 157/157 通过，release SHA-256 为 `a5c68da071b61c91393d32bd286ac220b8f14d780ddaa29ba32f911c05f848c2`；版本内嵌完整 commit `be91f9e0dac3b0a1f963aade71d8fb45c67a7529`。
- `be91f9e0` 把 CRI config 与 CreateSandbox request 的每个可变 map/list 编码为带 domain、count 和 entry/item marker 的 v2 TLV 流；对抗向量证明旧编码中可互换的 `annotations/linux-parent/sysctls` 语义现在产生不同指纹，Host lifecycle V12 返回 conflict 且 durable target/fingerprint 不变。
- 同一提交把 x86_64 Linux 6.6 数值 `pids.max` 上界固定为 `PID_MAX_LIMIT=4194304`；node-policy 在任何 Host controller I/O 前拒绝更大值，Host preflight 再做同一上界防御。`u64::MAX` 拒绝和四个 controller 文件零写入均有回归。
- forward WAL 对 9 次逻辑持久化逐次覆盖 `temp/file-fsync/rename/parent-fsync`；rollback 对 10 次逻辑持久化做同样覆盖。四个 controller 均覆盖 forward/rollback write、readback 与 step mark 边界。
- `RESTORED` 内嵌 journal 删除覆盖四个原子写阶段；cleanup queue unlink 覆盖 unlink 前、unlink 后未 fsync 两个 crash 点及 NotFound 重试 fsync。
- rollback 外部写前重新验证 epoch/identity/target；`revoked_epoch_fences_controller_rollback_before_external_write` 证明 durable `UndoIntent` 后 revoke 不产生 stale controller write。
- RuntimeClass CPU minimum 使用 checked 交叉乘法；`200000/49999` 等分数边界拒绝，`cpu_shares=0` 仅进入 fingerprint、不参与 ceiling。
- 本地 `cargo fmt --check`、offline `cargo check --tests`、`git diff --check` 与 `make handoff-validate` 通过；本机缺少动态 `libseccomp`，可执行测试以固定 builder 结果为准。

## 恢复决策表逐行映射

| 冻结决策行 | 可复现测试/故障点 | 结论 |
|---|---|---|
| PREPARED/SERVER_IDENTIFIED/SOCKET_READY，helper 原身份存活且未超时 | `socket_ready_does_not_treat_normal_helper_exit_as_abandonment`、`scanner_cannot_revoke_a_newer_first_rpc_claim`、双节点 watchdog gate 200 | scanner 观察，不创建 controller WAL |
| 同上，helper 死亡或 30 秒超时 | `scanner_publishes_failure_before_cleaning_abandoned_claim`、`scanner_adopts_only_full_nonce_executable_argv_bundle_match` | 新 epoch revoke、双 owner durable handoff、cleanup-only |
| TAKEOVER_CLAIMED + IN_PROGRESS，server 持当前 epoch | `first_rpc_claim_is_retryable_across_every_atomic_record_stage`、`delete_fences_takeover_before_host_commit`、S3.4c.2 placement/response crash 矩阵 | 只完成 Host placement；controller/RuntimeResource 尚不可分配 |
| TAKEOVER_CLAIMED + FAILED | `failed_create_is_not_drained_until_last_waiter_exits`、`scanner_publishes_failure_before_cleaning_abandoned_claim` | 不再 forward，drained/30 秒后 cleanup |
| CONTAINERD_COMMITTED + IN_PROGRESS，身份与 containment 匹配 | 9×4 forward persistence、21 个 forward write/readback/step crash 点；真实 `after-write-memory.oom.group` 调用 `inv-986fbag33m` | controller 依 actual=old/target 唯一恢复；`CONTROLLERS_COMMITTED` 前 RuntimeResource=EMPTY、VM=0 |
| CONTAINERD_COMMITTED + SUCCEEDED | `create_readback_success_linearizes_before_later_cleanup`、`create_readback_rejects_cleanup_revoked_between_inspect_and_return`；真实 V1 `inv-386f8tg1kc`/`inv-a86f940j5r` | 只 exact readback；parent 不写，leaf/WAL/RuntimeResource 一致 |
| CONTAINERD_COMMITTED + FAILED | `delete_during_create_publishes_failure_before_cleanup`；真实 crash 后 create=`FAILED`，清理 `inv-b86fci0he6`/`inv-686fct0t83` | 不再 allocate，只反向 rollback/release |
| 任一 pre-cleanup，server 死亡/immutable mismatch/containment breach | `scanner_fences_and_handoffs_before_waiting_for_operation_barrier`、`cleanup_signals_stuck_server_before_operation_barrier`、`containment_drift_cannot_commit_runtime_intent`、`revoked_epoch_fences_controller_rollback_before_external_write` | E→E+1 后旧 writer 所有副作用均被 fence |
| CLEANUP_REQUIRED/OWNERS_DURABLE/SERVER_STOPPED | 10×4 rollback persistence、16 个 rollback write/readback crash 点、`cleanup_does_not_enable_runtime_consumer_before_operation_barrier`、`independent_cleanup_queues_work_without_lifecycle_source` | controller 只 rollback、RuntimeResource 只 release；journal/queue 可幂等删除 |
| controller actual 既非 old 也非 target | `controller_rollback_recovers_intent_readback_and_degrades_on_foreign_value` | durable `DEGRADED`，不猜写、不 allocate |

## 云端验收

- 最终双 Worker 部署：`inv-886ftngie9`、`inv-886ftp00b4`，均为 `be91f9e0`/`a5c68da0…`、watchdog gate 200，部署前 sandbox/VM/lifecycle/queue/active lease 为零。
- 最终 RuntimeClass：`inv-v86fuwg3vq`，handler=`cube`、250m/256Mi、`cubesandbox.io/runtime=cube` nodeSelector、`勿删` 注解，且两个 prepared Worker Ready。
- 最终 V1：`inv-886fv7gh79` 创建；`inv-886fvfgqhw` 证明 parent=`max/weight1/max/max` 且只读，leaf=`125000 100000/536870912/512/1`，WAL=`CONTROLLERS_COMMITTED`，RuntimeResource/TAP/VM=`ALLOCATED`；`inv-086fvqg040` 删除临时 namespace 后，`inv-686fw1g9bw`/`inv-a86fw20wua` 双 Worker 精确归零。
- 最终真实 crash：`inv-986fbag33m` 在 `memory.oom.group` durable `INTENT`、actual=target、RuntimeResource=EMPTY、VM=0 时终止精确 server；`inv-686fct0t83` cleanup-only rollback 后 journal/record/queue/VM/sandbox 全部删除；`inv-b86fdng3d5` 进一步证明 active lease 也为零。
- 既有固定向量保持有效：V6 低 overhead `inv-086e27gf2q`、V10 高 overhead `inv-n86e6ggwvg`/`inv-886e70g6da`、V8 in-place resize `inv-686e92g43x`/`inv-v86e9pgeaj`/`inv-p86eacgvd1`/`inv-986eatgidd`，各自 cleanup 均已精确归零。
- 最终集群审计 `inv-386fwf0qi3`：control 与两个 Worker 均为 Kubernetes v1.36.4/Ready，两个 Worker 为 Linux 6.6 PVM、containerd 2.3.4，apiserver `/readyz=ok`，无 E2E 临时 namespace、Pod 或进程残留。

## 非阻断记录

- V10 首次从 Docker Hub 拉取镜像超时，改用腾讯云镜像后通过；属于外部 registry 可用性，不是 runtime 失败。
- 预检时误把 `e2e_node.test --version` 当作只读版本查询，二进制实际开始运行 specs；调用已立即取消，`inv-686ensg7hk` 证明无残留 Pod/进程、节点与 apiserver 健康。后续 runner 禁止以 `--version` 探测，并在执行前固定 dry-run 参数。
- `inv-v86fnd0gc8` 在下载 bundle 前因编排端用 `sed` 注入带 `&` 的签名 URL 导致 403；未创建 worktree、未运行测试、未触碰集群。改用不解释 replacement 元字符的注入方式后，`inv-386fp40a6b` 一次通过。

## 结论

实现与证据已覆盖 S3.4c.3 的 fixed vectors、controller transaction、恢复决策、真实 RuntimeClass/resize 和零残留要求；同一 reviewer 已明确返回 `APPROVE S3.4c.3`。S3.4c.3 关闭，当前进入 S3.4c.4。
