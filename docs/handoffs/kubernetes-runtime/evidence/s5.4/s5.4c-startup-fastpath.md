# S5.4c 非快照启动优化验收

## 结论

S5.4c `DONE`。实现 commit 为 `fa278467d5bf7100eadd3a3004b19446f87d6dcb`，tree 为
`d118d11a2a0ff4074458a7c3ecb795e03f2eb85a`。普通启动以单次 systemd D-Bus placement 加
parent/leaf inode、`/proc/<pid>/cgroup` 和 lifecycle epoch 作为一致性门禁，不再在正常路径派生
`systemctl show`。串行性能满足 S5.4a 冻结的 Node E2E 入口门槛；并发成功率为 100%。

同一 reviewer 最终结论为 `PASS`：P0=0、P1=0、P2=1、无必须修改项。P2 是建议以后为
placement 失败分支及 watchdog 0/2 精确候选增加纯函数负向单测；节点 systemd probe、启动压力与
故障矩阵已经执行新路径，因此不阻断本阶段。

## 实现与构建

- `StartTransientUnit` 成功后只调用一次 `verify_systemd_placement`，等待 kernel-visible leaf 与
  PID placement，并连续 5 次复核 parent inode、leaf inode 和精确 cgroup membership。
- 后续生命周期检查只复核持久化 inode 与 `/proc` membership；cleanup/诊断路径仍可使用
  `systemctl`。
- watchdog 通过固定 cgroup 的 `cgroup.procs` 找到唯一一个同时匹配 executable inode 与完整
  argv hash 的进程，并继续校验 boot ID、start time、当前二进制 SHA 和 probe stamp。
- 本地 `cargo fmt --check`、Host cgroup 78/78、完整 lib 288/288、worker process 1/1 和
  `git diff --check` 通过；一项既有锁时序测试曾抖动一次，单项与整组重跑均通过。
- release build：`inv-9896bxggdx`；源码 bundle：
  `s5.4/source/cubesandbox-94c01e21-to-fa278467.bundle`，SHA-256
  `32371a523b0072493b29f9ab99e7dceb7cdf9f5315741ea4663cd0d163274a37`。
- runtime 包：`s5.4/runtime/cubesandbox-runtime-fa278467.tar.gz`，SHA-256
  `eceba4a2b11a7ab15838453ed9a4b6c1ccfc1efc643ec5b2510911cba7e09f0e`。
- 二进制 SHA-256：Shim `dbb52f29efdd9bfcf48cbd65ca5c2b8a324524d4ba92b42ff3fe9ae5bc3b684a`；
  worker `693ecffd75b66caccff4db7a363a11a72ee6b55b3001f2f8b144e02f7e7189f6`；
  runtime `9aac8c8d0eb1d39fbb5fadf36832b36f72d04d79ab305d26a378ef200644e395`。
- W2 部署：`inv-0896gx0uqt`；回滚目录：
  `/opt/cubesandbox-s0-multinode-runtime-2269a3b3/backups/s54-fastpath-20260904T211438Z`。

## 热路径与镜像门禁

W2 的短时 containerd process trace `inv-b896kd0dc3` 覆盖完整 Pod 创建窗口：

```text
EXECVE_TOTAL=5
SYSTEMCTL_TOTAL=0
SYSTEMCTL_SHOW_TOTAL=0
```

固定镜像为 `mirror.ccs.tencentyun.com/library/busybox:1.36.1`；image content ID 为
`sha256:b116e155074440ffd9e449559433feb4cd2341eb3554b1da1c638c976e56451d`，repo digest 为
`sha256:73aaf090f3d85aa34ee199857f03fa3a95c8ede2ffd4cc2cdb5b94e566b11662`，见
`inv-0897120tmu`、`inv-38971a0q7v`。测试使用 `imagePullPolicy: Never`，kubelet 指标中的
`PullImage` 增量为 0。

## 串行与并发结果

主口径由同一 runner 使用 `CLOCK_MONOTONIC` 记录第一次观察到 `PodScheduled=True` 到第一次
观察到 `Ready=True`；CRI 口径由逐样本 kubelet `RunPodSandbox` sum/count 增量取得，分位数为
nearest-rank。

串行 50 次 `inv-8896px02qi` 全部成功，证据目录为
`/data/cubelet/s5.4-evidence/s5.4c-serial-20260904T212206Z`：

| 口径 | P50 | P95 | P99 | S5.4c 门槛 |
|---|---:|---:|---:|---:|
| PodScheduled→Ready | 1911.587 ms | 1935.438 ms | 1949.250 ms | P95 ≤ 2500 ms |
| RunPodSandbox | 1553.762 ms | 1575.632 ms | 1592.374 ms | P95 ≤ 2000 ms |
| create accepted→Ready | 1911.551 ms | 1935.398 ms | 1949.215 ms | 参考值 |

5 轮、每轮 10 个并发 Pod 的 `inv-a896ubgp5c` 也是 50/50 成功、PullImage=0；证据目录为
`/data/cubelet/s5.4-evidence/s5.4c-concurrent-20260904T212647Z`。并发
PodScheduled→Ready P50/P95/P99 为 2719.128/2907.534/2970.607 ms，五轮 RunPodSandbox
均值为 2236.777～2415.862 ms。S5.4a 对并发入口只冻结成功率门禁，因此通过；该结果同时量化了
普通启动离最终一秒目标的差距，S5.4d 仍需以 template/restore 快路径关闭。

## 生命周期回归与清理

- 多容器、emptyDir、containerd/RuntimeResource restart：`inv-6896vkg2f1`、
  `inv-8896vq0h35`、`inv-b896vt0di3`、`inv-3896vwgmi8`；Shim/worker PID 保持不变。
- worker SIGKILL：`inv-3896wcg3bu`、`inv-0896we0txv`、`inv-8896wg0n42`；Pod 变为
  Failed/Error，`inv-6896wj0w5s` exact-zero。
- Shim SIGKILL：`inv-9896wt0ssh`、`inv-v896wugbga`、`inv-3896wwgjba`；worker 同步退出，
  Pod 非 Ready，`inv-9896x1g473` exact-zero。
- 5 Pod 创建中取消：`inv-6896x80ii2` 在 5 秒删除 namespace，`inv-0896xd0qnv`
  exact-zero。
- 串行与并发结束后的 exact-zero 分别为 `inv-9896u50atv`、`inv-8896v9gi18`；最终检查中的
  adapter/shared/reaper/cleanup/mount/active lease/Shim/worker/VM/TAP/scope 全为 0。

S5.4c 冒烟期间确认 Guest 缺少 `/etc/hostname`；Pod 本身正常 Ready，问题转 S5.3b 的
`K8S-OQ-033`，不作为本阶段性能回归。
